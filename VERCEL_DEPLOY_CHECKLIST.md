# Vercel 部署检查清单

## ⚠️ 错误：找不到"页面"或"应用程序"目录

这个错误表示 Vercel 无法找到 `app/` 目录。按照以下步骤逐一检查：

## 📋 步骤 1：初始化 Git 仓库（如果尚未初始化）

在项目根目录 `e:\LTNS\` 执行：

```bash
git init
git add .
git commit -m "Initial commit: Complete Next.js project"
```

## 📋 步骤 2：验证所有必需文件都存在

确保以下文件和目录都在项目中：

```
✅ app/page.tsx          - 主页面
✅ app/layout.tsx        - 布局文件
✅ app/globals.css       - 全局样式
✅ package.json          - 项目配置
✅ next.config.ts        - Next.js 配置
✅ tsconfig.json         - TypeScript 配置
✅ vercel.json           - Vercel 配置（可选）
```

## 📋 步骤 3：创建并推送 Git 仓库

### 3.1 在 GitHub/GitLab/Bitbucket 创建新仓库

### 3.2 连接到远程仓库并推送

```bash
git remote add origin <你的仓库URL>
git branch -M main
git push -u origin main
```

## 📋 步骤 4：在 Vercel 中配置项目

### 4.1 导入项目

1. 登录 [Vercel Dashboard](https://vercel.com/dashboard)
2. 点击 "Add New" → "Project"
3. 选择你的 Git 仓库
4. 点击 "Import"

### 4.2 检查项目设置

**重要设置：**

- **Framework Preset**: `Next.js`（应自动检测）
- **Root Directory**: `./` 或留空（确保是项目根目录）
- **Build Command**: `npm run build`（默认）
- **Output Directory**: 留空（Next.js 自动处理）
- **Install Command**: `npm install`（默认）

### 4.3 配置环境变量

在 "Environment Variables" 中添加：

```
OPENAI_API_KEY = sk-your-deepseek-api-key-here
```

⚠️ **重要**：确保使用的是 DeepSeek API 密钥，不是 OpenAI 的密钥！

### 4.4 部署

点击 "Deploy" 按钮

## 📋 步骤 5：如果部署仍然失败

### 5.1 检查构建日志

在 Vercel Dashboard → Deployments → 点击失败的部署 → 查看日志

常见问题：
- ❌ "找不到 app 目录" → 检查 Git 仓库中是否包含 `app/` 目录
- ❌ "找不到 package.json" → 检查根目录设置
- ❌ "构建命令失败" → 检查本地 `npm run build` 是否成功

### 5.2 验证 Git 仓库内容

在你的 Git 仓库网页上检查：
- 能看到 `app/` 目录吗？
- 能看到 `package.json` 吗？
- 能看到所有源文件吗？

如果看不到 `app/` 目录，说明文件没有被提交到 Git。

### 5.3 重新提交文件

```bash
# 确保所有文件都被跟踪
git add -A

# 检查状态
git status

# 提交
git commit -m "Ensure all files are committed"

# 推送
git push
```

## 📋 步骤 6：常见问题排查

### 问题 A：Git 仓库中没有 app 目录

**解决**：
```bash
git add app/
git commit -m "Add app directory"
git push
```

### 问题 B：Vercel 根目录设置错误

**解决**：
1. Vercel Dashboard → 项目 Settings → General
2. 将 "Root Directory" 设置为 `./` 或留空
3. 重新部署

### 问题 C：.gitignore 意外忽略了文件

**检查**：
```bash
git status --ignored
```

确保 `app/` 目录没有被忽略。

### 问题 D：本地构建成功但 Vercel 失败

**检查**：
1. Node.js 版本是否匹配（需要 18+）
2. 环境变量是否已配置
3. 构建日志中的具体错误信息

## ✅ 成功部署后检查

部署成功后，访问你的 Vercel URL，验证：
- [ ] 页面正常加载
- [ ] AI 聊天功能正常（需要 API 密钥）
- [ ] 数据可视化图表显示
- [ ] 历史时间线显示

## 🆘 仍需要帮助？

如果问题仍然存在，请提供：
1. Vercel 构建日志的完整错误信息
2. Git 仓库 URL（如果公开）
3. `git status` 的输出
4. 本地 `npm run build` 的结果


